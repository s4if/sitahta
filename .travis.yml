language: php
php:
  - 5.4
  - 5.6
  - hhvm
  
before_script:
  - mysql -e 'SET sql_mode='ANSI_QUOTES';create database sitahta;'
  - mysql -e "use sitahta;CREATE TABLE IF NOT EXISTS user ('nip' varchar(15) NOT NULL, 'password' varchar(512) NOT NULL, 'nama' varchar(40) NOT NULL, PRIMARY KEY ('nip'));"
  - mysql -e "INSERT INTO user ('nip', 'password', 'nama') VALUES ('1', 'd8578edf8458ce06fbc5bb76a58c5ca4', 'admin');"
  - echo "USE mysql;\nUPDATE user SET password=PASSWORD('zaraki') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root
  - composer install
  
script:
- phpunit #default test command
